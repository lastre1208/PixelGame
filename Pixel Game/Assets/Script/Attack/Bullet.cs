using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet: PixelAttack
{
    // Start is called before the first frame update
    AttackParameter attackParameter;

    // Update is called once per frame
    
}
